---
import Card from './ui/card.astro';
import { getCollection } from 'astro:content';

const projects = await getCollection('projects', ({ data }:any) => {
	return !data.inProgress;
});
---

<section
	data-testid="projects"
	id="projects"
	class="mb-10 flex flex-col items-center justify-center md:mb-0 md:h-screen"
>
	<h2 class="font-general dark:text-primary-400 text-heading-4">Projects</h2>
	<span class="mb-10 h-1 w-10 bg-orange"></span>
	<div class="mb-10 flex w-full flex-col flex-wrap items-center md:flex-row">
		{
			projects.map((project:any) => (
				<Card
					url={project.data.link}
                    image={project.data.image}
					title={project.data.title}
					description={project.data.description}
					alt={project.data.img_alt}
                    live={project.data.live}
                    github={project.data.github}
					tags={project.data.tags}
                    appImages={project.data.appImages}
				/>
			))
		}
	</div>
	<a
		href="https://github.com/coleburch"
		target="_blank"
		rel="noopener"
		class="flex flex-row text-black hover:text-orange dark:text-white dark:hover:text-orange"
		aria-label="link to repository"
	>
		View my GitHub with more projects
		<svg
			xmlns="http://www.w3.org/2000/svg"
			fill="none"
			viewBox="0 0 24 24"
			stroke-width="1.5"
			stroke="currentColor"
			class="ml-1 h-6 w-6"
		>
			<path
				stroke-linecap="round"
				stroke-linejoin="round"
				d="M17.25 8.25L21 12m0 0l-3.75 3.75M21 12H3"></path>
		</svg>
	</a>
</section>