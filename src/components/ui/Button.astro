---
interface Props {
	size?: 'sm' | 'md' | 'lg' | 'xl';
	block?: boolean;
	style?: 'primary';
	class?: string;
	[x: string]: any;
}

const { size = 'md', style = 'primary', block, class: className, ...rest } = Astro.props;

const sizes = {
	sm: 'px-4 py-1.5',
	md: 'px-5 py-2.5',
	lg: 'px-6 py-3',
    xl: 'px-8 py-4 text-body-1',
};

const styles = {
	primary: 'bg-black text-white hover:bg-accent-500  border-2 border-transparent',
};
---

<button
	{...rest}
	class:list={[
		'rounded-xl text-center transition focus-visible:ring-2 ring-offset-2 ',
		block && 'w-full',
		sizes[size],
		styles[style],
		className,
	]}
	><slot />
</button>